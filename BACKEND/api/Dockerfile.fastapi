FROM python:3.12-slim

WORKDIR /app

COPY BACKEND/api/requirements-unified.txt /app/requirements.txt
RUN pip install --no-cache-dir -r /app/requirements.txt

COPY . /app
ENV PYTHONPATH=/app

EXPOSE 8001
CMD ["uvicorn", "BACKEND.api.main:app", "--host", "0.0.0.0", "--port", "8001"]
